apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ska-sci-ops-setup-validator-backend.fullname" . }}-test-connection"
  labels:
    {{- include "ska-sci-ops-setup-validator-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ska-sci-ops-setup-validator-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
